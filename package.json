{
  "name": "memento",
  "version": "1.0.0",
  "private": true,
  "description": "Memento web app",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "start": "cd server && npm install && node index.js",
    "backend-dev": "cd server && npm install && npm start",
    "frontend-dev": "cd client && npm install && npm start",
    "build": "cd client && npm install && npm run build"
  }
}
