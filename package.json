{
  "name": "memento",
  "version": "1.0.0",
  "private": true,
  "description": "Memento web app",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "start": "node server/index.js",
    "frontend-dev": "cd client && npm install && npm start",
    "backend-dev": "cd server && npm install && npm start",
    "build": "cd client && npm install && npm run build && rm -rf node_modules",
    "clean": "rm -rf client/node_modules && rm -rf client/build && rm -rf server/node_modules"
  }
}
